# Dependencies / build artifacts (never commit)
node_modules/
.venv/
venv/
__pycache__/
dist/
build/
target/
*.log

# Project output is never committed — conversions run inside this clone,
# in these gitignored areas
/workspaces/
analysis/
sources/
mendix/
knowledge-base/
*.mpr
*.mpr.backup
*.mpr.lock
CLAUDE.local.md
.claude/agents/

# WIP skills — machine-local drafts, not ready to publish
wip/

# OS noise
.DS_Store
